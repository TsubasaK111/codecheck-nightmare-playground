# installed NodeJS modules, which are often very large.
node_modules/

# byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# because macOS is macOS.
.DS_Store

# because logs and tempfiles are annoying and instance-specific.
*.log
temp_*